export enum CommandName {
  VERSION = '--version',
  HELP = '--help',
  IMPORT = '--import',
  GENERATE = '--generate',
}

export const DEFAULT_DB_PORT = '27017';
export const DEFAULT_USER_PASSWORD = '123456';
